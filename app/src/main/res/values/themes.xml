<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppBienvenidos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppBienvenidos" parent="Base.Theme.AppBienvenidos" />

    <style name="AppTitleStyle">
        <item name="android:textColor">#FBC02D</item>   <!-- couleur du texte (violet) -->
        <item name="android:textStyle">bold</item>        <!-- gras -->
        <item name="android:letterSpacing">0.05</item>    <!-- espacement entre les lettres -->
        <item name="android:shadowColor">#55000000</item> <!-- petite ombre pour relief -->
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">2</item>
        <item name="fontFamily">cursive</item>
    </style>

    <style name="RoundedCorners">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>
</resources>